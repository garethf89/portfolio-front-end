
name: ESLint

on:
  push:
    branches: [ master ]

  workflow_dispatch:

jobs:
  eslint:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v2
        
      - name: Install modules
        run: yarn
        
      - name: ESLint
        run: yarn lint:js
